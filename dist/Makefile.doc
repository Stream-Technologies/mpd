# $Id: Makefile.doc,v 1.3 2003/02/10 22:23:20 archiecobbs Exp $

PREFIX?=	/usr/local
BINDIR?=	${PREFIX}/share/doc/mpd

MPD_DOC_FILES+=	../README
MPD_DOC_FILES+=	mpd*.html
MPD_DOC_FILES+=	mpd.ps

MAN8=		mpd.8
MANDIR=		${PREFIX}/man/man

depend:

clean cleandir:
	rm -f mpd.8

all:		mpd.8

mpd.8:          mpd.8.in
	sed -e "s,@PREFIX@,${PREFIX},g" < ${.ALLSRC} > ${.TARGET}

.if !defined(NOPORTDOCS)
afterinstall:
	[ -d ${BINDIR} ] || mkdir ${BINDIR}
	${INSTALL} -c -o ${DOCOWN} -g ${DOCGRP} -m ${DOCMODE} ${MPD_DOC_FILES} \
		${BINDIR}
.endif

.include <bsd.prog.mk>
