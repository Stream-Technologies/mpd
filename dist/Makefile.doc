# $Id: Makefile.doc,v 1.1 2000/11/05 00:33:48 archiecobbs Exp $

PREFIX?=	/usr/local
BINDIR?=	${PREFIX}/share/doc/mpd

MPD_DOC_FILES+=	../README
MPD_DOC_FILES+=	mpd*.html

MAN8=		mpd.8
MANDIR=		${PREFIX}/man/man

depend:

clean cleandir:
	rm -f mpd.8

all:		mpd.8

mpd.8:          mpd.8.in
	sed -e "s,@PREFIX@,${PREFIX},g" < ${.ALLSRC} > ${.TARGET}

.if !defined(NOPORTDOCS)
afterinstall:
	${INSTALL} -c -o ${DOCOWN} -g ${DOCGRP} -m ${DOCMODE} ${MPD_DOC_FILES} \
		${BINDIR}
.endif

.include <bsd.prog.mk>
