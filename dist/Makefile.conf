# $Id: Makefile.conf,v 1.2 2002/01/15 18:21:04 archiecobbs Exp $

PREFIX?=	/usr/local
BINDIR?=	/etc/mpd4

MPD_SAMPLES+=	mpd.conf.sample
MPD_SAMPLES+=	mpd.links.sample
MPD_SAMPLES+=	mpd.secret.sample
MPD_SAMPLES+=	mpd.script.sample
NOOBJ=

all clean cleandir depend:

afterinstall:
	${INSTALL} -c -o ${BINOWN} -g ${BINGRP} -m ${SHAREMODE} ${MPD_SAMPLES} \
		${PREFIX}${BINDIR}

.include <bsd.prog.mk>
