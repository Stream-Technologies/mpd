# $Id: Makefile.conf,v 1.1 2000/11/05 00:33:47 archiecobbs Exp $

PREFIX?=	/usr/local
BINDIR?=	/etc/mpd

FILES+=		mpd.conf.sample
FILES+=		mpd.links.sample
FILES+=		mpd.secret.sample
FILES+=		mpd.script.sample
NOOBJ=

all clean cleandir depend:

afterinstall:
	${INSTALL} -c -o ${BINOWN} -g ${BINGRP} -m ${SHAREMODE} ${FILES} \
		${PREFIX}${BINDIR}

.include <bsd.prog.mk>
